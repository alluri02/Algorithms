package com.example.patterns.structural.flyweight;

/**
 * Flyweight Pattern - Flyweight interface
 * Defines the interface for flyweights
 */
public interface TreeType {
    void render(int x, int y, String color, String size);
}
